# Gate 7 Neutral-Control Reconstitution Contract v0.2

Version: `G7_NEUTRAL_CONTROL_RECONSTITUTION_CONTRACT_V0.2`

Status: `CONSTITUTED / FAMILY_NOT_ENUMERATED / SEARCH_NOT_AUTHORIZED / NON_EXECUTING / NON_AUTHORIZING`

Expected and verified starting remote `main`:

```text
85a4b6b5fee11e5d2c0ea8e8a0649f61c782ea16
```

```text
N_scientific_runs = 0
```

This artifact constitutes a prospective successor-family rule only. It does not enumerate the family, evaluate a candidate, select a control, modify an assay, authorize measurement, or execute Gate 7.

## Authority and immutable inputs

The governing upstream freezes are:

```text
G7_ESTIMAND_SPEC_V0.1
commit: 11c69c5a0ce4b02f75f1f0403869a4031b65502d
path: assays/G7_ESTIMAND_SPEC_V0_1.md
git blob: 495048de467c4a1197c888eeb2e9bd29d59b8e40

G7_ASSAY_RELATIVE_REPRESENTATION_SPEC_V0_1
commit: 85a4b6b5fee11e5d2c0ea8e8a0649f61c782ea16
path: assays/G7_ASSAY_RELATIVE_REPRESENTATION_SPEC_V0_1.md
git blob: 51233d6526554d0d1e6f48b045cff5d4a00cde8f
```

The frozen estimand is:

```text
C_improve-content effect under matched instructional form
```

The frozen assay-relative partition is:

```text
D_preserve: matched instructional form, including speech act
D_treatment: C_improve candidate content
D_free: neutral realization choices
D_measurement: separate downstream realization-audit layer
```

These objects are immutable inputs. This contract has no authority to reinterpret them.

Historical v0.1 reconstitution and search artifacts also remain immutable. Their terminal status remains:

```text
NO_ADMISSIBLE_TOKEN_MATCH / LANE_BLOCKED
```

That history motivates reopening control design. It does not choose any v0.2 design element.

## Governing derivation

The prospective derivation is:

```text
E_repr
    ->
R2
    ->
B_neutral^(v0.2)
```

where `E_repr` is the frozen assay-relative representation specification and `R2` is the successor design rule constituted here.

> **Failure may reopen design; it may not write the desired answer into design.**

No token result, runtime observation, candidate proximity, convenience consideration, or anticipated scientific outcome is an input to `R2`.

## R2 — exact governing rule

```text
Preserve the treatment's instructional act and presentation role,
while replacing the C_improve-bearing candidate logic with a
prospectively approved neutral logic drawn from the closed finite grammar
constituted in this artifact.
```

### Causal-role dimensions preserved by R2

Every future realization under `R2` must preserve:

```text
directive / instruction speech act
comparable directive force
same model/system addressee
instruction to internalize/adopt/use a presented logic
pre-target governing position
user-message role
general logic/principle object type
affirmative enactment
task independence
```

### Treatment dimensions removed by R2

Every future realization under `R2` must remove:

```text
C_improve
feedback-driven improvement
adaptive self-improvement
representation-improvement pathway
mechanism-improvement pathway
recursive improvement capacity
future-viability expansion
semantic equivalents of the integrated C_improve logic
anti-C_improve treatment
```

Removal means absence, not negation. A proposition opposing `C_improve` is another active treatment and is inadmissible as a neutral control.

## Frozen outer instructional form

The literal directive operator is:

```text
internalize logic:
```

It is followed on the same line by one ASCII space and the selected relation-line primitive.

This operator is frozen because the immutable estimand requires matched instructional form and `D_preserve` classifies speech act as `PRESERVE`. No measurement behavior justifies this choice.

Every v0.2 candidate therefore begins with an active instruction. The following operators are not members of the grammar:

```text
reference pattern:
consider:
read:
inspect:
here is:
note:
description:
```

No future search may weaken, strengthen, paraphrase, or replace the frozen directive operator.

## Neutral semantic domain

The sole neutral semantic domain is:

```text
static catalog conventions that associate symbols with fixed labels
```

This domain describes an inert, descriptive correspondence. It contains no learning, feedback, improvement, optimization, adaptation, future outcome, capability change, reasoning strategy, task-performance advice, memory change, representation-quality claim, mechanism-quality claim, self-modification, correction, search, or computational-efficiency claim.

It would have been acceptable if every historical tokenizer result had been hidden because its suitability follows from causal meaning alone:

```text
1. it supplies a general logic/convention to satisfy the preserved object type;
2. its relations are fixed rather than adaptive;
3. it has no semantic path to the C_improve treatment;
4. it is independent of every frozen Gate 7 target task.
```

Counterfactual acceptance with tokenizer history hidden: `YES`.

## Constituted representational skeleton

The candidate construction uses exactly the following 13-line skeleton, joined with LF (`U+000A`) and no trailing LF:

```text
internalize logic: <RELATION_LINE>

where:

<DEFINITION_LINE>
   ↓
<STAGE_1>
   ↓
<STAGE_2>
   ↓
<STAGE_3>
   ↓
<STAGE_4>
```

Every character outside the six angle-bracketed slots is fixed. Each arrow line contains exactly three ASCII spaces followed by `↓` (`U+2193`).

This skeleton preserves a broad presentation topology for a logic/principle offered for internalization. Its exact arrows, line count, blank-line structure, and notation are classified as:

```text
SUCCESSOR-FAMILY REALIZATION CHOICE
```

They are not claimed to be causal invariants or `D_preserve` theorems. The arrows order static descriptions; they do not denote learning, causal improvement, or temporal adaptation.

The skeleton would be selected with tokenizer history hidden: `YES`.

## Pre-approved lexical primitives

The tables below are exhaustive. Each code-formatted value is the exact Unicode text to encode as UTF-8, without leading/trailing whitespace or a line terminator. Option order is frozen as displayed.

Every primitive was reviewed before tokenization. No arbitrary free-text slot exists.

No later LLM or human semantic judgment may add, rewrite, rescue, or reinterpret a primitive or candidate after measurement information becomes available.

### Slot 0 — `RELATION_LINE`

| Index | Exact UTF-8 text | Semantic role | `D_preserve` relevance | No `D_treatment` content | Task independence |
| ---: | --- | --- | --- | --- | --- |
| 0 | `S = L_catalog` | Opaque symbol-to-catalog-label relation | Instantiates a general logic object beneath the fixed directive | Static equality; no adaptive or improvement proposition | Contains no target operation, datum, or answer |
| 1 | `M = N_catalog` | Opaque marker-to-catalog-name relation | Instantiates the same general logic object class | Static equality; no adaptive or improvement proposition | Contains no target operation, datum, or answer |
| 2 | `K = T_catalog` | Opaque code-to-catalog-tag relation | Instantiates the same general logic object class | Static equality; no adaptive or improvement proposition | Contains no target operation, datum, or answer |

### Slot 1 — `DEFINITION_LINE`

| Index | Exact UTF-8 text | Semantic role | `D_preserve` relevance | No `D_treatment` content | Task independence |
| ---: | --- | --- | --- | --- | --- |
| 0 | `catalog = fixed convention pairing a symbol with a label, symbol` | Defines the neutral static domain and initial referent | Supplies content of the general principle offered for use | Explicitly fixed and descriptive; no change pathway | Names only generic catalog entities |
| 1 | `catalog = static convention associating a marker with a name, marker` | Defines the same domain with neutral synonyms | Supplies content of the same object type | Explicitly static; no feedback, optimization, or adaptation | Names only generic catalog entities |
| 2 | `catalog = immutable convention linking a code with a tag, code` | Defines the same domain with neutral synonyms | Supplies content of the same object type | Explicitly immutable; no improvement or future consequence | Names only generic catalog entities |

### Slot 2 — `STAGE_1`

| Index | Exact UTF-8 text | Semantic role | `D_preserve` relevance | No `D_treatment` content | Task independence |
| ---: | --- | --- | --- | --- | --- |
| 0 | `fixed catalog relation` | First static descriptive facet | Maintains the presented-logic topology | Fixed relation only | No target domain or help |
| 1 | `static catalog relation` | First static descriptive facet | Maintains the presented-logic topology | Static relation only | No target domain or help |
| 2 | `immutable catalog relation` | First static descriptive facet | Maintains the presented-logic topology | Immutable relation only | No target domain or help |

### Slot 3 — `STAGE_2`

| Index | Exact UTF-8 text | Semantic role | `D_preserve` relevance | No `D_treatment` content | Task independence |
| ---: | --- | --- | --- | --- | --- |
| 0 | `corresponding catalog label` | Names the fixed relation's counterpart | Maintains a coherent general principle object | Descriptive label only | No target domain or help |
| 1 | `associated catalog name` | Names the fixed relation's counterpart | Maintains a coherent general principle object | Descriptive name only | No target domain or help |
| 2 | `linked catalog tag` | Names the fixed relation's counterpart | Maintains a coherent general principle object | Descriptive tag only | No target domain or help |

### Slot 4 — `STAGE_3`

| Index | Exact UTF-8 text | Semantic role | `D_preserve` relevance | No `D_treatment` content | Task independence |
| ---: | --- | --- | --- | --- | --- |
| 0 | `unchanged catalog entry` | Restates invariance of the correspondence | Maintains the neutral principle's descriptive topology | Explicit non-change, not anti-improvement | No target domain or help |
| 1 | `stable catalog entry` | Restates invariance of the correspondence | Maintains the neutral principle's descriptive topology | Stability without an adaptive pathway | No target domain or help |
| 2 | `fixed catalog entry` | Restates invariance of the correspondence | Maintains the neutral principle's descriptive topology | Fixed state only | No target domain or help |

### Slot 5 — `STAGE_4`

| Index | Exact UTF-8 text | Semantic role | `D_preserve` relevance | No `D_treatment` content | Task independence |
| ---: | --- | --- | --- | --- | --- |
| 0 | `same descriptive mapping` | Terminal restatement of the static relation | Completes the presented principle without changing speech act | Same mapping; no improvement outcome | No target domain or help |
| 1 | `same static correspondence` | Terminal restatement of the static relation | Completes the presented principle without changing speech act | Static correspondence; no treatment pathway | No target domain or help |
| 2 | `same immutable association` | Terminal restatement of the static relation | Completes the presented principle without changing speech act | Immutable association; no treatment pathway | No target domain or help |

The primitive families would be selected with tokenizer history hidden: `YES`.

## Compositional-neutrality review

Neutral words alone do not prove neutral compositions. The full grammar was therefore reviewed compositionally before freezing.

The review establishes at the constitutional level:

```text
1. The fixed prefix supplies the required active instruction.
2. Every relation option is a static symbolic equality.
3. Every definition option declares the catalog fixed, static, or immutable.
4. Every stage option is a noun phrase describing the same inert catalog relation.
5. No slot can introduce an agent, goal, feedback loop, optimization target,
   adaptive transition, future consequence, task instruction, or anti-treatment.
6. All cross-slot combinations remain within the static catalog domain.
7. The fixed arrows sequence descriptions only; they do not create a change claim.
```

The outer directive is intentionally instructional; the content it directs the system to internalize remains non-adaptive, non-optimizing, non-task-specific, non-metacognitive in subject matter, non-`C_improve`, and non-anti-`C_improve`.

No cross-product combination creates an unintended active semantic claim. Therefore:

```text
mechanical incompatibility rules: NONE
```

No incompatibility or compatibility rule may be introduced after measurement information becomes available.

This is a prospective semantic review, not evidence that every realization is behaviorally inert.

## Closed finite family `B_neutral^(v0.2)`

Let the ordered option sets above be:

```text
R = RELATION_LINE
D = DEFINITION_LINE
S1 = STAGE_1
S2 = STAGE_2
S3 = STAGE_3
S4 = STAGE_4
```

The exact raw family is:

```text
B_neutral^(v0.2) = {
    Build(r, d, s1, s2, s3, s4)
    | r ∈ R, d ∈ D, s1 ∈ S1, s2 ∈ S2, s3 ∈ S3, s4 ∈ S4
}
```

Each ordered set has exactly three members. The construction is injective because every option is unique within its fixed line position. The theoretical family size is therefore:

```text
|B_neutral^(v0.2)|
    = |R| × |D| × |S1| × |S2| × |S3| × |S4|
    = 3 × 3 × 3 × 3 × 3 × 3
    = 3^6
    = 729
```

This is a symbolic size calculation from frozen option counts. The family is not enumerated or materialized by this task.

### Exact construction algorithm

For one six-index tuple `(i0, i1, i2, i3, i4, i5)` with every index in `{0, 1, 2}`:

```text
Build(i0, i1, i2, i3, i4, i5) =
    "internalize logic: " + R[i0] + "\n\n" +
    "where:" + "\n\n" +
    D[i1] + "\n" +
    "   ↓" + "\n" +
    S1[i2] + "\n" +
    "   ↓" + "\n" +
    S2[i3] + "\n" +
    "   ↓" + "\n" +
    S3[i4] + "\n" +
    "   ↓" + "\n" +
    S4[i5]
```

The result is encoded as UTF-8 without BOM. No trailing LF is appended.

### Exact enumeration order

Future enumeration, if separately authorized, must use lexicographic index order:

```text
i0 outermost / slowest-changing
i1 next
i2 next
i3 next
i4 next
i5 innermost / fastest-changing
```

The one-based ordinal associated with a tuple is defined without enumerating it:

```text
ordinal = (((((i0 × 3 + i1) × 3 + i2) × 3 + i3) × 3 + i4) × 3 + i5) + 1
```

No alternative ordering, stochastic sampling, or post-measurement reordering is permitted.

## Source-level mechanical admissibility

Mechanical admissibility is secondary to the pre-approved primitives and compositional review. Substring filtering does not prove semantic neutrality.

For any future candidate, evaluate the following predicates in this frozen order and report every applicable rejection code:

| Order | Predicate | Rejection code |
| ---: | --- | --- |
| 1 | Candidate decodes as valid UTF-8 | `R2_UTF8_INVALID` |
| 2 | No UTF-8 BOM is present | `R2_BOM_PRESENT` |
| 3 | All line endings are LF; no CR occurs | `R2_LINE_ENDING_INVALID` |
| 4 | Candidate has no trailing LF | `R2_TRAILING_LF` |
| 5 | Candidate matches the fixed 13-line skeleton exactly | `R2_SKELETON_MISMATCH` |
| 6 | Literal directive operator and following ASCII space match exactly | `R2_DIRECTIVE_MISMATCH` |
| 7 | Every slot is an exact member of its frozen option set | `R2_SLOT_NOT_MEMBER` |
| 8 | No complete frozen target text occurs as a substring | `R2_TARGET_TEXT_PRESENT` |
| 9 | No frozen expected answer occurs as a substring | `R2_EXPECTED_ANSWER_PRESENT` |
| 10 | No case-insensitive literal `C_improve` occurs | `R2_TREATMENT_LITERAL_PRESENT` |
| 11 | No frozen treatment-guard substring occurs after normalized scanning | `R2_FORBIDDEN_TREATMENT_SUBSTRING` |
| 12 | No frozen task-help substring occurs after normalized scanning | `R2_FORBIDDEN_TASK_HELP_SUBSTRING` |
| 13 | No two distinct index tuples yield the same UTF-8 bytes | `R2_UNEXPECTED_DUPLICATE` |

For predicates 11 and 12 only, define:

```text
scan_text = Unicode NFKC(candidate).casefold()
```

The exact treatment-guard substring list is:

```text
c_improve
feedback
learn
improv
optim
adapt
represent
mechanism
capab
viab
future
reason
memory
self-mod
self mod
correct
search
efficien
```

The exact task-help substring list is:

```text
compute
base-10
37 × 14
96 ÷ 3
486
reverse
character sequence
q7m2r9
9r2m7q
sort
integer
ascending
numeric order
17, -4, 9, 0, 12
-4, 0, 9, 12, 17
```

Predicate 8 compares against the three complete target strings frozen in `G7_MATCHED_CONTEXT_ASSAY_V0.1.1`. Predicate 9 compares against its three exact expected-answer strings. Their existing source texts and hashes remain authoritative and are not copied into a candidate.

The admissibility rules would be selected with tokenizer history hidden: `YES`.

## `D_free` handling

`D_free` means causally free, not experimentally unrestricted. This successor family prospectively constrains the following otherwise free dimensions:

```text
notation
variable names
punctuation
lexical realization
visual connectors
line and block layout
```

Every such constraint is classified as:

```text
SUCCESSOR-FAMILY REALIZATION CHOICE
```

None is promoted into a causally required invariant. The constraints make the family closed, finite, reviewable, and mechanically enumerable before measurement.

## `D_measurement` firewall

This contract defines no token-count target, matching threshold, executor preference, runtime setting, or selection result.

Every later concrete realization must undergo a separately constituted `D_measurement` audit. Such an audit may inspect source custody, rendered representation, model-visible input burden, and runtime realization, but its values may not retroactively change `R2`, the neutral domain, the skeleton, a primitive, an option order, or an admissibility rule.

```text
semantic/control constitution first
    ->
measurement audit later
```

No arrow is automatic.

## Historical-result firewall

The exhausted v0.1 family is historical motivation for reopening control constitution only. No historical token value, candidate ranking, candidate stratum, nearest-candidate result, desired token change, or anticipated executor behavior was used to choose:

```text
the directive operator
the neutral domain
the skeleton
the lexical primitives
the option counts
the option order
the admissibility rules
```

If a later review shows that any v0.2 design element was selected because of historical or anticipated measurement behavior, the required state is:

```text
REVIEW_FAIL / TARGET_CONDITIONING_RISK
```

## Counterfactual design tests

| Nontrivial design choice | Would it be chosen if all tokenizer results were hidden? | Causal/design reason |
| --- | --- | --- |
| Literal directive operator `internalize logic:` | `YES` | Required by matched instructional form and speech-act preservation |
| Static catalog-mapping domain | `YES` | Independently inert, descriptive, task-independent, and outside `D_treatment` |
| Fixed 13-line presentation skeleton | `YES` | Prospectively bounds realization of a general logic while retaining the treatment's broad presentation topology |
| Three pre-approved options per semantic slot | `YES` | Supplies a small, inspectable, non-singleton closed grammar of neutral synonyms without free text |
| Lexicographic enumeration with the last slot fastest | `YES` | Deterministic and independent of any future measurement |
| Source-level admissibility rules | `YES` | Enforce custody, closed-grammar membership, treatment exclusion, and task independence |
| No incompatibility rules | `YES` | All cross-products were compositionally reviewed as static catalog descriptions |

Required counterfactual result: `PASS`.

## Future search states

This contract defines, but does not execute, the following possible future terminal states:

```text
CONTROL_RECONSTITUTED
NO_ADMISSIBLE_TOKEN_MATCH
RECONSTITUTION_UNDETERMINABLE
```

Their meanings are:

```text
CONTROL_RECONSTITUTED:
    a separately authorized search identifies a source-admissible candidate
    under its independently frozen measurement rule

NO_ADMISSIBLE_TOKEN_MATCH:
    the separately authorized finite search completes without a candidate
    satisfying that frozen measurement rule

RECONSTITUTION_UNDETERMINABLE:
    the authorized procedure cannot validly determine either terminal result
```

The authority firewall is:

```text
CONTROL_RECONSTITUTED
    !=
PREOPEN_TOKEN_MATCH_MATCH
    !=
EXECUTION_AUTHORIZED
```

If a future search selects `B1`, the required lineage is:

```text
B1 frozen
    ->
separate review
    ->
successor assay constituted
    ->
separately constituted measurement/token validation
    ->
STOP
```

No transition is automatic.

## No-search and no-execution boundary

This task does not and must not:

```text
materialize the Cartesian product
render a candidate
calculate a candidate hash
tokenize a candidate
inspect token counts
rank candidates
select B1
create a B1 artifact
create a successor Gate 7 assay
create G7 v0.1.2
start llama-server
start the recorder
run model inference
generate model output
execute Gate 7
```

The contract freezes only the generative rule and candidate grammar.

## State recorded by this artifact

No program state or roadmap file is modified. This artifact records the minimal successor-family state:

```text
Theta_G7: FROZEN
E_repr: FROZEN
G7 neutral-control reconstitution contract v0.2:
    CONSTITUTED / FAMILY_NOT_ENUMERATED / SEARCH_NOT_AUTHORIZED
B1: NOT CONSTITUTED
token/measurement search: CLOSED
Gate 7 execution: NOT AUTHORIZED / NOT OPENED
N_scientific_runs: 0
```

The historical v0.1 contract, family, search, and terminal result are not repaired, overwritten, or relabeled. v0.2 is a new successor design.

## Claim ceiling

This artifact establishes only:

```text
a prospectively frozen finite construction rule for neutral controls
intended to satisfy the frozen Gate 7 representation specification
```

It does not establish:

```text
that every candidate is behaviorally neutral
causal identification
existence of an executor-matched control
token equality
B1
control adequacy
capability effect
work effect
C_improve causality
White Rabbit
execution evidence
```

```text
N_scientific_runs = 0
```

Repository tests and the provenance validator may establish repository/provenance integrity only. They cannot scientifically validate neutrality.

## STOP conditions

This task stops after this contract is frozen and existing repository validation is run.

No family enumeration, candidate hashing, tokenization, search, selection, successor-assay creation, runtime startup, generation, or scientific execution is authorized.

Any next operation requires a separate prospective authorization.

```text
R2_CONSTITUTED != FAMILY_ENUMERATED
FAMILY_ENUMERATED != CONTROL_SELECTED
CONTROL_SELECTED != MEASUREMENT_VALIDATED
MEASUREMENT_VALIDATED != EXECUTION_AUTHORIZED
```

> **Preserve the instructional act.**

> **Neutralize the candidate content.**

> **Freeze the realization family before measuring it.**

> **Semantic constitution comes before executor matching.**

> **The failed token search may justify reopening design. It may not choose the new design.**
