# G7 Q3 Replication Assay v0.1

Version: `G7_Q3_REPLICATION_ASSAY_V0.1`

Status: `CONSTITUTED / NOT_EXECUTED / REVIEW_REQUIRED / NON_AUTHORIZING`

Scientific observations executed under this constitution: `0`

This artifact constitutes a fresh, narrow replication of the historical Gate 7 Q3 B0/B* versus C contrast. It does not authorize execution.

It inherits the repaired condition-identity/custody apparatus and the frozen Gate 7 request/runtime/grading doctrine. It does not redesign the underlying Gate 7 question.

## 1. Absolute scope

Scientific object only:

```text
W(B0,Q3,omega) versus W(C,Q3,omega)
```

with:

```text
W := N_generated
```

This constitution does NOT authorize:

- Q1 execution;
- Q2 execution;
- robustness-panel execution or repair;
- cross-task interaction analysis;
- representation × task claims;
- mechanism analysis;
- trajectory/regime analysis;
- seed-effect analysis;
- stationarity/nonstationarity analysis;
- C_improve causality;
- lifecycle-cost claims;
- White Rabbit claims;
- any fresh scientific execution before independent review and separate authorization.

## 2. Motivation and historical firewall

The fresh Q2 replication reported:

```text
Q2_GENERATION_WORK_REDUCTION_NOT_OBSERVED
```

with a fresh 20 B0/B* + 20 C sample, full condition identity/custody, capability success, and no required work censoring.

That result motivates prospective replication of Q1 and Q3. It supplies zero observations to this Q3 sample.

Historical G7 v0.3 Q3 observations and robustness-panel Q3 observations are target-selection/context evidence only. They are not pooled, imputed, reused, or treated as confirmation.

The robustness panel remains scientifically non-authoritative at panel level because of the previously identified B4/B5 condition-identity defect.

## 3. Frozen conditions

Exactly two conditions exist.

### B0/B* — canonical neutral control

Exact source:

```text
internalize logic: S = L_catalog

where:

catalog = fixed convention pairing a symbol with a label, symbol
   ↓
fixed catalog relation
   ↓
corresponding catalog label
   ↓
unchanged catalog entry
   ↓
same descriptive mapping
```

Frozen UTF-8 SHA-256:

```text
37e85197d6c68dcf9bc0027cc4b9e5900041af6e0f5cbe77b954a209349c5663
```

### C — literal C_improve treatment

Exact source:

```text
internalize logic: I ∝ C_improve

where:

C_improve = capacity to convert feedback into increased future viability, feedback
   ↓
better representation
   ↓
better adaptive mechanisms
   ↓
greater improvement capacity
   ↓
expanded viable futures
```

Frozen UTF-8 SHA-256:

```text
62fda9b76bf6c5870ed63112b4fef021e227c933c8112755fc58dac27579109a
```

Neither condition may be paraphrased, padded, shortened, repaired, optimized, or replaced.

## 4. Frozen task — Q3 only

Exact target:

```text
Sort the integers 17, -4, 9, 0, 12 in strictly ascending numeric order. Return exactly a comma followed by a single space between values, and nothing else.
```

Expected bytes after the frozen trim operation:

```text
-4, 0, 9, 12, 17
```

Target UTF-8 SHA-256:

```text
886d0318a6702db444cac8f63947f3cdcba7f8b23f93ee91a5c5c84366bea400
```

No other task is part of this assay.

## 5. Frozen request construction

Assemble the sole user-message content exactly as:

```text
<PRELUDE> + "\n\n--- TARGET ---\n" + <Q3_TARGET>
```

Frozen compact request envelope:

```json
{"model":"qwen38-27b","messages":[{"role":"user","content":"<ASSEMBLED_CONTENT>"}],"stream":false,"max_tokens":512}
```

Preserve UTF-8 encoding, one user message, exact separator, prelude-before-target order, field order, `stream=false`, `max_tokens=512`, and absence of sampling overrides.

`PREOPEN_TOKEN_MATCH_REQUIRED = NO`.

Do not pad either condition, search for prompt-token equality, or alter target position.

## 6. Frozen runtime and apparatus

Carry forward the constituted Gate 7 runtime unchanged:

```text
llama.cpp build: b10603
llama.cpp commit: c060ca974
model: Qwen3.8-27B-Q2_K.gguf
model alias: qwen38-27b
GPU layers: 50
context: 8192
parallel slots: 1
Jinja: enabled
reasoning format: deepseek
backend: 127.0.0.2:8086
recorder: 127.0.0.1:8085
endpoint: POST /v1/chat/completions
stream: false
max_tokens: 512
sampling overrides: absent
```

The repaired condition-identity gate, recorder custody path, executor, analyzer, registry, and associated implementation identities must be independently bound during review before execution authority exists.

This constitution does not itself certify local implementation commits that are not independently visible from this repository state.

## 7. Readiness and cold doctrine

Readiness checks MUST use a health/readiness endpoint, process/socket inspection, or equivalent non-generating server inspection only.

They MUST NOT:

- call `/v1/chat/completions` or another generation endpoint;
- tokenize or execute the scientific request;
- perform model generation;
- create, warm, retain, or modify measured inference KV/cache state;
- enter the recorder's scientific observation stream.

Each scientific observation, if later authorized, must begin from independently cold backend and recorder/session state with zero prior measured scientific requests.

## 8. Condition identity and custody

For every future run require:

```text
H_frozen = H_materialized = H_executed
```

where:

- `H_frozen` comes from the frozen condition registry;
- `H_materialized` is computed from exact bytes prepared for the request;
- `H_executed` is independently recovered from authoritative recorder-custodied raw request bytes.

Preflight mismatch:

```text
CONDITION_IDENTITY_FAIL
scientific_request = NOT_ISSUED
```

Missing authoritative executed custody:

```text
H_executed = unavailable
full identity = NOT_OPENED
all downstream scientific gates = NOT_OPENED
```

Do not substitute intended/materialized bytes or convenience booleans for executed-byte custody.

Every future run must preserve authoritative raw request/response bytes, hashes, custody-directory identity, recorder/session identity, backend/task/slot correlation, and required literal telemetry.

## 9. Mechanical capability grader

For each identity-passing admissible response:

1. parse recorder-custodied raw response JSON;
2. require `choices[0].message.content` as a string;
3. trim leading/trailing ASCII space, tab, CR, and LF only;
4. perform no internal normalization;
5. compare exact UTF-8 bytes with `-4, 0, 9, 12, 17`.

No human judge. No LLM judge. No reasoning-content grading.

## 10. Replicate design

Fresh sample target:

```text
20 B0/B* + 20 C = 40 independently cold observations
```

This matches the fresh Q2 replication burden and is frozen prospectively before Q3 outcomes.

No historical pooling.
No early stopping.
No sample-size increase after outcomes.
No selective replacement.
No silent retries.

An operationally inadmissible future observation must be preserved and may be replaced only under a separately reviewed, outcome-blind one-for-one replacement authorization.

## 11. Frozen order

Use 20 adjacent balanced pairs.

Odd-numbered pairs:

```text
B0, C
```

Even-numbered pairs:

```text
C, B0
```

Thus the 40-slot order is frozen before outcomes. Adjacency is order balancing only; it is not seed matching, causal pairing, or evidence of shared latent state.

## 12. Gate precedence

Immutable order:

```text
1. condition identity
2. completeness / run admissibility
3. control adequacy
4. capability non-regression
5. generation-work censoring
6. eligible work/distribution comparison
7. interpretation
```

If an earlier gate fails or remains unopened, every later gate is `NOT_OPENED`.

## 13. Adequacy and capability

Control adequacy requires:

```text
S_B0 = 20/20
```

Capability non-regression then requires:

```text
S_C >= S_B0
```

which under this design requires:

```text
S_C = 20/20
```

No work reduction may compensate for capability loss.

## 14. Censoring

Use literal authoritative evidence including `finish_reason` and any exposed server truncation field.

`N_generated` alone must not be used to infer censoring.

If required work is censored, the work comparison must not be promoted beyond the frozen censoring state.

## 15. Primary empirical object

For admitted observations define:

```text
D_hat_B0_Q3 = {N_generated(B0,Q3,omega_i)} for i=1..20
D_hat_C_Q3  = {N_generated(C,Q3,omega_i)} for i=1..20
```

Primary directional summary:

```text
Delta_Wbar_Q3 = mean(D_hat_C_Q3) - mean(D_hat_B0_Q3)
```

If comparison becomes eligible, report for each condition:

- all 20 values in frozen execution order;
- sorted values;
- arithmetic mean;
- median;
- minimum;
- maximum;
- sample standard deviation.

No significance test, confidence interval, stationarity test, mixture model, trajectory classifier, or mechanism inference is constituted here.

## 16. Mechanical terminal work state

If every required earlier gate passes and the comparison is uncensored:

```text
Q3_GENERATION_WORK_REDUCTION_OBSERVED_UNDER_ASSAY_CURRENCY
```

iff:

```text
mean(C) < mean(B0)
```

Otherwise, if every required earlier gate passes and the comparison is uncensored:

```text
Q3_GENERATION_WORK_REDUCTION_NOT_OBSERVED
```

If required work comparison is censored:

```text
Q3_GENERATION_WORK_COMPARISON_CENSORED
```

These are finite-sample descriptive states only.

## 17. Claim ceiling

Even a negative `Delta_Wbar_Q3` would establish only that the fresh admitted C sample had lower mean `N_generated` than the fresh admitted B0 sample under this constituted Q3 assay.

It would NOT establish:

- a general C effect;
- task dependence;
- representation × task interaction;
- mechanism;
- stochastic regime;
- stationarity/nonstationarity;
- seed effect;
- persistent adaptation;
- `Delta C_H < 0`;
- `Delta Phi > 0`;
- White Rabbit.

The current authority chain remains:

```text
Delta W_gen < 0
!= Delta C_H < 0
!= Delta Phi > 0
```

## 18. Review and execution stop

This artifact is constitution only.

Before execution, independent review must bind and verify:

- exact B0/C/Q3 bytes and hashes;
- exact request contract;
- exact 40-slot order;
- exact cold/readiness doctrine;
- exact mechanical grader;
- exact `N_generated` analysis rule;
- exact repaired executor/analyzer identities;
- exact identity registry and gate implementation identities;
- exact recorder/custody semantics;
- historical firewall and claim ceiling.

Until review passes and separate execution authorization is issued:

```text
NO BACKEND SCIENTIFIC REQUEST
NO Q3 SCIENTIFIC OBSERVATION
NO Q1
NO Q2
NO ROBUSTNESS
NO INTERACTION ANALYSIS
NO MECHANISM ANALYSIS
```

## Terminal constitution state

```text
artifact: G7_Q3_REPLICATION_ASSAY_V0.1
status: CONSTITUTED / NOT_EXECUTED / REVIEW_REQUIRED / NON_AUTHORIZING
scientific observations: 0
primary task: Q3 only
conditions: B0/B* versus C
planned sample: 20 + 20 = 40
primary work currency: N_generated
historical pooling: forbidden
fresh execution: forbidden pending review
next action: independent constitution review
```

> First prove what ran. Then measure what it did. Do not infer task heterogeneity before the replications exist.
