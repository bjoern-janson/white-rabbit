# Gate 7 Neutral-Control Robustness Assay v0.1

Version: `G7_NEUTRAL_CONTROL_ROBUSTNESS_ASSAY_V0.1`

Status: `CONSTITUTED / PANEL_FROZEN / NOT_EXECUTED / REVIEW_REQUIRED / NON_AUTHORIZING`

Scientific observations executed: `0`

## Authority and scope

Required starting remote `main`: `de09aac6811ee31467e5f852b0312b21a7ba2d87`.

Upstream result: `G7_MATCHED_CONTEXT_ASSAY_V0.3`, result commit `de09aac6811ee31467e5f852b0312b21a7ba2d87`.

Upstream neutral-family authority: `assays/G7_NEUTRAL_CONTROL_RECONSTITUTION_CONTRACT_V0_2.md` (`B_neutral^(v0.2)`, six lexical slots, three levels each, theoretical size `3^6 = 729`). Upstream artifacts remain unchanged.

This artifact constitutes a predeclared structural robustness panel. It does not enumerate the family, execute a request, authorize execution, or alter the Gate 7 design.

## Scientific question and claim ceiling

G7 v0.3 observed lower `N_generated` for literal `C_improve` than canonical `B*` under preserved mechanical task capability. The nearest surviving alternative is dependence on that particular neutral realization.

This assay asks whether the local generation-work advantage survives six prospectively frozen, structurally diverse controls from the closed family. The panel is not random, representative, exhaustive, or a population sample. It does not estimate a family mean, random-neutral probability, or population-average effect.

The strongest permitted positive claim is conditional on the frozen executor, tasks, `N_generated` currency, and all six panel members. No claim about all 729 controls, other tasks/models, persistent adaptation, `C_improve -> Phi`, `Delta W_run`, `Delta C_H`, or White Rabbit is authorized.

## Immutable design

Preserve unchanged: `Theta_G7`, `E_repr`, `R2`, `E_measurement`, literal treatment `C`, Q1/Q2/Q3 wording and answers, mechanical grader, executor, tokenizer, template, reasoning format, sampling policy, cold-state and recorder doctrines, `N_prompt = MEASURE`, `W_gen = N_generated`, censoring, ledger separation, and claim ceiling. No token matching, padding, new vocabulary, or control search.

Exactly seven conditions are defined: `B0`, `B1`, `B2`, `B3`, `B4`, `B5`, and literal `C`.

### Frozen panel

| Label | Tuple | R2 ordinal | UTF-8 SHA-256 |
| --- | --- | ---: | --- |
| B0 (`B*`) | `(0,0,0,0,0,0)` | 1 | `37e85197d6c68dcf9bc0027cc4b9e5900041af6e0f5cbe77b954a209349c5663` |
| B1 | `(1,1,1,1,1,1)` | 365 | `1d61d0a91ce5f33b10494ca8d1eb762689247b4dece248b3b1f01a615850e156` |
| B2 | `(2,2,2,2,2,2)` | 729 | `a22143e334a1fbf0c5296549c8c97e7eea82fe03af974ed10098d96cca769486` |
| B3 | `(0,1,2,0,1,2)` | 141 | `67d18099551eec4aebbb4f6112c55f1d9fc2a24d1a6833093e9de8849d5d84cf` |
| B4 | `(1,2,0,1,2,0)` | 421 | `43a4f288c6707e64ee984be5d7177e5a95a6ccf97040ae7b8fe171638c39878b` |
| B5 | `(2,0,1,2,0,1)` | 533 | `ac3f9a8d90133f9b14754f43b03ed6e9e842e752739c396cc6f84418553d7c0c` |

The ordinal equation is `((((((i0*3+i1)*3+i2)*3+i3)*3+i4)*3+i5)+1`. Only these six tuples are materialized; the 729-member family is not enumerated. Mechanical reconstruction with the exact R2 tables verified all six hashes above. B0 is canonical `B*`: **YES**.

### Exact reconstructed controls

```text
B0
internalize logic: S = L_catalog

where:

catalog = fixed convention pairing a symbol with a label, symbol
   ↓
fixed catalog relation
   ↓
corresponding catalog label
   ↓
unchanged catalog entry
   ↓
same descriptive mapping
```

```text
B1
internalize logic: M = N_catalog

where:

catalog = static convention associating a marker with a name, marker
   ↓
static catalog relation
   ↓
associated catalog name
   ↓
stable catalog entry
   ↓
same static correspondence
```

```text
B2
internalize logic: K = T_catalog

where:

catalog = immutable convention linking a code with a tag, code
   ↓
immutable catalog relation
   ↓
linked catalog tag
   ↓
fixed catalog entry
   ↓
same immutable association
```

```text
B3
internalize logic: S = L_catalog

where:

catalog = static convention associating a marker with a name, marker
   ↓
immutable catalog relation
   ↓
corresponding catalog label
   ↓
stable catalog entry
   ↓
same immutable association
```

```text
B4
internalize logic: M = N_catalog

where:

catalog = immutable convention linking a code with a tag, code
   ↓
fixed catalog relation
   ↓
associated catalog name
   ↓
unchanged catalog entry
   ↓
same descriptive mapping
```

```text
B5
internalize logic: K = T_catalog

where:

catalog = fixed convention pairing a symbol with a label, symbol
   ↓
static catalog relation
   ↓
linked catalog tag
   ↓
fixed catalog entry
   ↓
same static correspondence
```

### Selection rationale

Selection uses only frozen grammar structure, tuple symmetry, and lexical-level balance. Across the six tuples, every lexical slot contains each level exactly twice. The panel contains all-0, all-1, all-2 diagonals and three cyclic mixed realizations. It is not selected using token counts, prompt length, latency, generation work, v0.3 outcomes, behavioral performance, semantic preference after execution, or anticipated treatment advantage. No maximal-diversity, random-sampling, representativeness, or 729-coverage claim is made.

### Treatment and tasks

Literal `C` is unchanged; SHA-256 `62fda9b76bf6c5870ed63112b4fef021e227c933c8112755fc58dac27579109a`.

Tasks are the frozen G7 tasks: Q1 expected `486`; Q2 expected `9R2m7Q`; Q3 expected `-4, 0, 9, 12, 17`. Wording and target hashes are inherited unchanged from `G7_MATCHED_CONTEXT_ASSAY_V0.3`.

Request serialization is unchanged: `<PRELUDE> + "\n\n--- TARGET ---\n" + <TARGET>` in one user message, compact UTF-8 JSON, `stream:false`, `max_tokens:512`. Executor is unchanged: llama.cpp `b10603` / `c060ca974`, Qwen3.8-27B-Q2_K.gguf, alias `qwen38-27b`, GPU 50, context 8192, one slot, Jinja enabled, deepseek reasoning, backend `127.0.0.2:8086`, recorder `127.0.0.1:8085`, recorder v0.1.0 reported commit `80cddb26a7b851d218f95317cd3c5b0593acd831`, sampling overrides absent.

## Fresh observations and cold doctrine

No historical G7 v0.2, calibration, or v0.3 observation enters this assay. The design is `7 conditions × 3 tasks × 5 independently cold replicates = 105 fresh observations`. Every observation requires a new backend PID, new recorder/session, startup snapshot, zero prior measured requests, first-slot cold/LRU evidence, exactly one request and measurement block, exact correlation, no retained cross-run KV/cache, and exact request/response byte/hash custody. No execution is authorized here.

`N_prompt = MEASURE`; token matching is not required. Missing literal backend fields remain missing; cached tokens must not be inferred from `graphs_reused`.

## Frozen execution schedule

Condition numbers: `1=B0, 2=B1, 3=C, 4=B2, 5=B3, 6=B4, 7=B5`.

The 14-row library is frozen exactly as:

```text
R01 1 2 7 3 6 4 5    R02 2 3 1 4 7 5 6
R03 3 4 2 5 1 6 7    R04 4 5 3 6 2 7 1
R05 5 6 4 7 3 1 2    R06 6 7 5 1 4 2 3
R07 7 1 6 2 5 3 4    R08 5 4 6 3 7 2 1
R09 6 5 7 4 1 3 2    R10 7 6 1 5 2 4 3
R11 1 7 2 6 3 5 4    R12 2 1 3 7 4 6 5
R13 3 2 4 1 5 7 6    R14 4 3 5 2 6 1 7
R15 = R01
```

The first 14 rows mechanically have each condition in each serial position twice and each ordered adjacent distinct pair twice. The exact 15-block assignment is frozen below; each block is independent cold execution, not shared state:

| Block | Rep | Task | Runs | Row | Condition order |
|---:|---:|---|---|---|---|
| 01 | 1 | Q1 | 001–007 | R15 | B0 B1 B5 C B4 B2 B3 |
| 02 | 1 | Q2 | 008–014 | R04 | B2 B3 C B4 B1 B5 B0 |
| 03 | 1 | Q3 | 015–021 | R03 | C B2 B1 B3 B0 B4 B5 |
| 04 | 2 | Q1 | 022–028 | R02 | B1 C B0 B2 B5 B3 B4 |
| 05 | 2 | Q2 | 029–035 | R07 | B5 B0 B4 B1 B3 C B2 |
| 06 | 2 | Q3 | 036–042 | R12 | B1 B0 C B5 B2 B4 B3 |
| 07 | 3 | Q1 | 043–049 | R14 | B2 C B3 B1 B4 B0 B5 |
| 08 | 3 | Q2 | 050–056 | R13 | C B1 B2 B0 B3 B5 B4 |
| 09 | 3 | Q3 | 057–063 | R08 | B3 B2 B4 C B5 B1 B0 |
| 10 | 4 | Q1 | 064–070 | R10 | B5 B4 B0 B3 B1 B2 C |
| 11 | 4 | Q2 | 071–077 | R09 | B4 B3 B5 B2 B0 C B1 |
| 12 | 4 | Q3 | 078–084 | R06 | B4 B5 B3 B0 B2 B1 C |
| 13 | 5 | Q1 | 085–091 | R05 | B3 B4 B2 B5 C B0 B1 |
| 14 | 5 | Q2 | 092–098 | R01 | B0 B1 B5 C B4 B2 B3 |
| 15 | 5 | Q3 | 099–105 | R11 | B0 B5 B1 B4 C B3 B2 |

The supplied schedule's task-local position check was mechanically evaluated before freezing. Required range is 3.8–4.2 inclusive for every condition and exact C mean 4.0. **Result: FAIL / REVIEW BLOCKER.** Under the frozen assignment, C means are Q1 `4.2`, Q2 `3.6`, Q3 `4.2`; several other condition/task means also fall outside the required range. The schedule is preserved exactly as instructed; this artifact does not repair or reinterpret it. Execution is not authorized while this inconsistency remains unresolved.

## Gates and precedence

Precedence is immutable: (1) completeness/admissibility; (2) six-control panel adequacy, requiring every B0–B5/task cell `5/5`; (3) fresh C capability non-regression, Q1/Q2/Q3 `5/5`; (4) censoring; (5) six pairwise comparisons; (6) panel terminal state. Censoring is `ROBUSTNESS_WORK_COMPARISON_CENSORED` only after earlier gates pass. Pairwise PASS for each B_i requires taskwise `mean(N_generated_C,j) <= mean(N_generated_Bi,j)` for all tasks and pooled C mean strictly lower. Possible panel states are `ROBUST_CONTROL_REALIZATION_ADVANTAGE_OBSERVED`, `CONTROL_REALIZATION_DEPENDENCE_OBSERVED`, and `ROBUST_CONTROL_REALIZATION_ADVANTAGE_NOT_OBSERVED`.

Report five literal `N_generated` values and mean/median/min/max per condition/task, taskwise and pooled C-minus-control differences, descriptive percentages, and exact passing controls. No p-values or significance language. Keep `Delta W_gen != Delta W_run != Delta C_H`.

## Absolute non-execution boundary

This artifact is constitution only. Do not start llama-server or recorder, call endpoints, tokenize for matching, evaluate output, inspect new behavior, create observations, or emit any scientific or White Rabbit result. Historical G7 v0.3 remains unchanged. Scientific requests: `0`.

## Completion record

- artifact: `assays/G7_NEUTRAL_CONTROL_ROBUSTNESS_ASSAY_V0_1.md`
- starting local HEAD: `049e108c706df51fe083ed1e2ed3fc91a633a6e0`
- required starting remote main: `de09aac6811ee31467e5f852b0312b21a7ba2d87`
- panel: six-control structural robustness panel
- tuples/ordinals/hashes: verified as tabulated above
- slot levels 0/1/2 balance: verified
- B0 canonical B*: YES
- C hash: verified
- conditions/tasks/replicates: `7 / 3 / 5`
- planned fresh observations: `105`
- first-14 position and adjacency balance: verified
- task-local position requirement: **NOT VERIFIED; mechanically failed under supplied schedule**
- `N_prompt`: MEASURE; token matching required: NO
- historical observations reused: NO; G7 v0.3 changed: NO
- llama-server started: NO; recorder started: NO; scientific requests: `0`
- unit tests and validator: run after artifact creation
- ambiguities/blockers: supplied schedule conflicts with its task-local position requirement; no repair made
- execution: NOT AUTHORIZED; STOP
