# Experimental Contract v0.1 — Calibration Result

Status: `CORRECTION_COST_SEPARATION_OBSERVED`

## Role

This was a **synthetic positive-control / knife-calibration** benchmark. The treatment deliberately created a correction-relevant structural difference: the productive history compiled and discarded source rules, while the comparison history compiled and retained them.

It therefore does **not** establish that useful accumulation naturally creates corrective debt.

## Capability gate

Under a fixed 64-work-unit capability budget:

```text
V(s0) = 21
V(sP) = 64
V(sC) = 64
```

Both accumulated histories improved the benchmark capability currency and were matched before surprise.

## Held-out surprise

A prospectively frozen family contained one rule flip among `G0..G7`. The selected surprise was:

```text
e* = G6 flips 0 -> 1
```

## Minimum independently validated correction cost

```text
C*(sP,e*) = 8
C*(sC,e*) = 2
Delta C*_(P:C) = +6
```

Both final corrected states passed the independent validator.

## Earned claim

> The apparatus can discriminate a genuinely existing correction-cost difference under prospectively frozen conditions.

Nothing stronger is authorized by this calibration result.
